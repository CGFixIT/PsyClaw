---
description: >-
  Behavioral-uplift, reasoning-discipline, AND knowledge-handoff layer for Chris (GitHub cgfixit) — epistemic calibration, premise-testing, self-review, security discipline, anti-sycophancy, correct Sonnet 5 vs Opus 5 model routing, his communication contract, project portfolio, and the CyClaw facts to know cold.
---

Apply the fable-protocol reasoning-discipline-and-knowledge layer to the current task. $ARGUMENTS

See `.claude/skills/fable-protocol/SKILL.md` for full detail — this file is a thin
pointer, not an independent copy, precisely so the two cannot drift the way they
did before (this file was found citing a stale scanner-pattern count and an
outdated "Opus 4.8" routing reference after the skill had already moved past both
— see PR #867).

**Scope note:** this protocol is registered both at `.claude/skills/fable-protocol/SKILL.md`
in this repo and at the user-level `~/.claude/skills/fable-protocol/SKILL.md`, so it
activates in any repository, not only CyClaw. It is scoped to the user, not to CyClaw
in origin, but its §8 does document CyClaw architecture, load-bearing numbers, and
settled decisions as a knowledge base to know cold — it carries no AUTHORITY over
either, though: `CLAUDE.md` and `config.yaml` remain the source of truth for both, and
this skill's §8 has no authority over the six invariants in `CLAUDE.md` §3 — those
still govern. It does not own life/career coaching — this is the reasoning-quality
and project-context layer beneath all technical work.

**Consolidation note (2026-09-06):** this skill absorbed the former companion
`fable-5.1-cc` knowledge-handoff skill (§8 onward in the SKILL.md is that content,
reconciled against this one). `fable-5.1-cc` and its command wrapper no longer exist
in this repo; see the SKILL.md's §11 for why.
